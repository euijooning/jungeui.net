#!/usr/bin/env bash
# Jungeui Labs - 배포 자동화 (예시)
# 사용: bash scripts/deploy.sh
# 실제 서버 경로·환경에 맞게 수정 후 사용하세요.

set -e

echo "[deploy] Jungeui Lab deploy script (placeholder)"

# 예시: 가상환경 활성화 후 API 재시작
# source .venv/bin/activate
# pip install -r requirements.txt
# uvicorn apps.api.main:app --host 0.0.0.0 --port 8000

# 예시: 프론트 빌드
# cd apps/backoffice && npm ci && npm run build
# cd ../client && npm ci && npm run build

echo "[deploy] Done. Configure this script for your environment."
